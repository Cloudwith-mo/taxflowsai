{
  "name": "taxflowsai-frontend",
  "version": "1.0.0",
  "scripts": {
    "build": "esbuild src/login.js --bundle --minify --define:API_BASE='\"https://dql2om6jc.execute-api.us-east-1.amazonaws.com/default\"' --outdir=build"
  },
  "dependencies": {
    "aws-sdk": "^2.1622.0",
    "amazon-cognito-identity-js": "^6.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.19.0"
  }
}

